#include<bits/stdc++.h>
using namespace std;

int main(){
	// dynamic size
	// rich library functions find ,erase,insert etc
	// easy to know the size v.size()
	// no need to pass the size as an argument
	// can be returned from a fucntion
	// by default initialized with default values
	// we can copy one array to other v1=v2 no need of looping every item
	



	return 0;
}