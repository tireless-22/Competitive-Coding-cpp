#include<bits/stdc++.h>
using namespace std;
int main (){
	int *p=0;
	// int *p=22343;
	// here we intialized the pointer
	// to zero, which means we are equalising 
	// with the null
	// pointer will never point the vallues
	// it only points the variables
	// but 0 is special thing which means it is null

	

	
	return 0;
}